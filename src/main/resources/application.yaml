spring:
  application:
    name: demoinyeccion
  profiles:
    default: local
  config:
    import: optional:classpath:feature-flags.yaml
logging:
  level:
    root: INFO
server:
  port: 8080
app:
  welcome-message: "Bienvenido"
